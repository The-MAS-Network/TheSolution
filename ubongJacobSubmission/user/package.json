{
  "name": "the-solution-web-app-mono-repo",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",

  "author": "ubongjacob.dev for the mas network competition",
  
  "scripts": {
    "backend:dev": "cd ./backend && yarn dev",
    "frontend:dev": "cd ./frontend && yarn dev",
    "dev": "npm-run-all --parallel backend:dev frontend:dev ",

    "migrate": "cd ./backend && yarn migrate",

    "backend:install": "cd ./backend && yarn",
    "frontend:install": "cd ./frontend && yarn",
    "install": "npm-run-all frontend:install backend:install",
    "build": "cd ./backend && yarn build && cd ../frontend && yarn build"
  },
  "license": "ISC",

  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
